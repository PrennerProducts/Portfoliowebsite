/** @type {import('next').NextConfig} */
const nextConfig = {
  // Hier können Sie Ihre anderen Next.js-Konfigurationen hinzufügen
  reactStrictMode: true,
  swcMinify: true,
  // Weitere Konfigurationen...
};

export default nextConfig;
